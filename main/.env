# ----------------------------------------------------
# 通用和全局配置 (General & Global Configuration)
# ----------------------------------------------------

# DashScope (通义千问) API Key
# 用于驱动所有大语言模型，包括意图识别和答案生成。
# 主要使用者:
# - models/Intent_recognition_model.py (QwenChat)
# - models/model_answer.py (DashScopeChatBot)
DASHSCOPE_API_KEY="sk-3b3a7e28a7c64d0f9382e89b4222abae"


# ----------------------------------------------------
# 数据库连接配置 (Database Connection)
# ----------------------------------------------------
# 这些配置被项目中的所有数据库交互模块使用。
# 主要使用者:
# - main/main.py
# - main/qa_engine.py
# - main/branch.py
# - main/branch_engine.py

DB_HOST=10.100.200.16
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_DATABASE=db
DB_CHARSET=utf8mb4


# ----------------------------------------------------
# 数据处理脚本专用配置 (Data Processing Script)
# ----------------------------------------------------
# 这些参数似乎是为某个数据处理或知识库生成脚本（可能名为 cs_123.py 或类似脚本）设计的，
# 用于从原始数据库表生成 `db/knowledge_base_final.json`。

# 从 items_info 表查询数据的SQL语句
CS_SQL_QUERY_TEST_TABLE=SELECT id, business_name FROM items_info_ ORDER BY id
# 从 items_doc 表查询数据的SQL语句
CS_SQL_QUERY_ITEMS_DOC=SELECT uid, doc_name, doc_text FROM items_doc_ ORDER BY uid

# 源数据表名
CS_TABLE_NAME_TEST=items_info_
CS_TABLE_NAME_ITEMS_DOC=items_doc_

# 生成的知识库文件输出路径
CS_OUTPUT_DIR=C:\Users\pc123\Desktop\新事心办-原 - 副本\db
# 生成的知识库文件名
CS_OUTPUT_FILENAME=knowledge_base_final.json


# ----------------------------------------------------
# 问答引擎专用配置 (QA Engine)
# ----------------------------------------------------
# 这些参数被核心问答流程使用，用于在运行时从数据库获取实时、详细的信息。
# 主要使用者:
# - main/main.py
# - main/qa_engine.py

# 根据ID查询业务详情和关联文档的SQL语句
QA_SQL_QUERY_ITEM_DETAILS=SELECT i.*, b.doc_name, b.doc_text FROM items_info_ i LEFT JOIN items_doc_ b ON i.id = b.uid WHERE i.id = %s
# 根据ID查询业务名称的SQL语句（用于生成多选项）
QA_SQL_QUERY_BUSINESS_NAME=SELECT id, business_name FROM items_info_ WHERE id = %s

# 业务信息表名
QA_TABLE_NAME_ITEMS_INFO=items_info_
# 业务关联文档表名
QA_TABLE_NAME_ITEMS_DOC=items_doc_